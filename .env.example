# Used by docker-compose.yml — copy to .env and adjust as needed.

APP_ID=local-app
APP_SECRET=change-me-to-a-long-random-string
MYSQL_ROOT_PASSWORD=greensheet_root

# Optional: live email sending. Leave blank to keep dispatch-ledger sends as "queued".
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM=
